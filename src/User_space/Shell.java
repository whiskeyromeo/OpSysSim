package User_space;

/**
 * @author Will Russell on 11/8/17
 * @project OS_Simulator
 *
 *  The shell should be initialized within the GUI and be able to perform a series of commands
 *  on the system
 *
 */
public class Shell {
    // Commands that the shell needs to be able to take in
    private final String COMMANDS[] = {"PROC","MEM","LOAD", "EXE", "RESET", "EXIT"};


}
